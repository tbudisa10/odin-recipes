First Odin Project Coded in HTML
HTML website in the following phases:

1. Basic HTML Structure (index.html root)
2. Main Pages (recipes pages)
3. Main Content (recipe content)
4. Refine

Remember to commit Git:

git add + git commit cycles before pushing it up
with push origin main

After you have entered git push origin main, switch over to your browser and open your repository on GitHub. You should now see all the files you just pushed